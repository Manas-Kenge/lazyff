---
title: "Installation"
description: "Detailed installation guide for all platforms"
---

## Prerequisites

### Modern Terminal (for TUI)

ffwrap includes an interactive TUI that works best with modern terminal emulators:

- [WezTerm](https://wezfurlong.org/wezterm/) (cross-platform)
- [Alacritty](https://alacritty.org/) (cross-platform)
- [Ghostty](https://ghostty.org/) (Linux, macOS)
- [Kitty](https://sw.kovidgoyal.net/kitty/) (Linux, macOS)
- [Windows Terminal](https://aka.ms/terminal) (Windows)
- iTerm2 (macOS)
- GNOME Terminal (Linux)
- Terminal.app (macOS)

<Note>
  CLI commands work in any terminal. The modern terminal recommendation is specifically for the interactive TUI mode with proper rendering and keyboard support.
</Note>

### ffmpeg

ffwrap requires ffmpeg (version 4.0 or higher) to be installed and available in your PATH.

<Tabs>
  <Tab title="macOS">
    **Using Homebrew (recommended):**
    ```bash
    brew install ffmpeg
    ```

    **Using MacPorts:**
    ```bash
    sudo port install ffmpeg
    ```
  </Tab>
  <Tab title="Ubuntu/Debian">
    ```bash
    sudo apt update
    sudo apt install ffmpeg
    ```

    For the latest version, use the official PPA:
    ```bash
    sudo add-apt-repository ppa:savoury1/ffmpeg4
    sudo apt update
    sudo apt install ffmpeg
    ```
  </Tab>
  <Tab title="Fedora">
    First, enable RPM Fusion:
    ```bash
    sudo dnf install https://mirrors.rpmfusion.org/free/fedora/rpmfusion-free-release-$(rpm -E %fedora).noarch.rpm
    ```

    Then install ffmpeg:
    ```bash
    sudo dnf install ffmpeg
    ```
  </Tab>
  <Tab title="Arch Linux">
    ```bash
    sudo pacman -S ffmpeg
    ```
  </Tab>
  <Tab title="Windows">
    **Using Chocolatey:**
    ```powershell
    choco install ffmpeg
    ```

    **Using Scoop:**
    ```powershell
    scoop install ffmpeg
    ```

    **Manual installation:**
    1. Download from [ffmpeg.org/download.html](https://ffmpeg.org/download.html)
    2. Extract to `C:\ffmpeg`
    3. Add `C:\ffmpeg\bin` to your PATH environment variable
  </Tab>
</Tabs>

Verify ffmpeg installation:
```bash
ffmpeg -version
```

## Install ffwrap

<Tabs>
  <Tab title="npm">
    ```bash
    npm install -g @ffwrap/cli
    ```
  </Tab>
  <Tab title="bun">
    ```bash
    bun install -g @ffwrap/cli
    ```
  </Tab>
  <Tab title="pnpm">
    ```bash
    pnpm add -g @ffwrap/cli
    ```
  </Tab>
  <Tab title="yarn">
    ```bash
    yarn global add @ffwrap/cli
    ```
  </Tab>
</Tabs>

## Verify Installation

Run the version command to verify everything is set up:

```bash
ffwrap version
```

Expected output:
```
ffwrap: 0.0.1
ffmpeg: 6.1.1 (or your version)
```

## Updating ffwrap

To update to the latest version:

<Tabs>
  <Tab title="npm">
    ```bash
    npm update -g @ffwrap/cli
    ```
  </Tab>
  <Tab title="bun">
    ```bash
    bun update -g @ffwrap/cli
    ```
  </Tab>
  <Tab title="pnpm">
    ```bash
    pnpm update -g @ffwrap/cli
    ```
  </Tab>
  <Tab title="yarn">
    ```bash
    yarn global upgrade @ffwrap/cli
    ```
  </Tab>
</Tabs>

## Uninstalling

To remove ffwrap:

<Tabs>
  <Tab title="npm">
    ```bash
    npm uninstall -g @ffwrap/cli
    ```
  </Tab>
  <Tab title="bun">
    ```bash
    bun remove -g @ffwrap/cli
    ```
  </Tab>
  <Tab title="pnpm">
    ```bash
    pnpm remove -g @ffwrap/cli
    ```
  </Tab>
  <Tab title="yarn">
    ```bash
    yarn global remove @ffwrap/cli
    ```
  </Tab>
</Tabs>

## Troubleshooting Installation

<AccordionGroup>
  <Accordion title="Command not found: ffwrap">
    This usually means the global bin directory isn't in your PATH.

    **For npm:**
    ```bash
    # Find npm's global bin directory
    npm bin -g

    # Add it to your PATH in your shell profile (.bashrc, .zshrc, etc.)
    export PATH="$(npm bin -g):$PATH"
    ```

    **For Bun:**
    ```bash
    # Add to your shell profile (.bashrc, .zshrc, etc.)
    export PATH="$HOME/.bun/bin:$PATH"
    ```
  </Accordion>
  <Accordion title="Command not found: ffmpeg">
    ffmpeg is not installed or not in your PATH.

    1. Verify ffmpeg is installed: `which ffmpeg` (Unix) or `where ffmpeg` (Windows)
    2. If not found, follow the installation steps above
    3. If installed but not found, add the ffmpeg binary location to your PATH
  </Accordion>
  <Accordion title="Permission denied errors">
    **On Unix systems:**
    ```bash
    # Don't use sudo with npm -g, fix permissions instead
    mkdir -p ~/.npm-global
    npm config set prefix '~/.npm-global'
    export PATH="$HOME/.npm-global/bin:$PATH"
    ```

    **Or use a version manager like nvm** which handles permissions correctly.
  </Accordion>
  <Accordion title="Codec not available errors">
    Some ffmpeg installations don't include all codecs.

    **macOS (reinstall with full codecs):**
    ```bash
    brew reinstall ffmpeg
    ```

    **Linux (install additional codecs):**
    ```bash
    # Ubuntu/Debian
    sudo apt install libavcodec-extra

    # Fedora
    sudo dnf install ffmpeg-libs
    ```
  </Accordion>
  <Accordion title="TUI not rendering correctly">
    If the TUI looks broken or has rendering issues:

    1. **Use a modern terminal** - See the terminal recommendations above
    2. **Check terminal encoding** - Ensure UTF-8 is enabled
    3. **Try a different terminal** - Some terminals have better support than others
    4. **Resize your terminal** - Make sure it's at least 80 columns wide
  </Accordion>
</AccordionGroup>

## Next Steps

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Try your first commands
  </Card>
  <Card title="TUI Mode" icon="window" href="/tui">
    Learn the interactive interface
  </Card>
</CardGroup>
